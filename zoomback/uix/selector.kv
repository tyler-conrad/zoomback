#:import ConfigModal zoomback.uix.modal.ConfigModal

<Selector>:
    cls: ['selector']

    BoxLayout:
        orientation: 'horizontal'
        
        Button:
            cls: ['config']
            text: 'Config'
            on_press: ConfigModal().open()
        Button:
            cls: ['filter']
            text: 'Filter'
        Button:
            cls: ['patch']
            text: 'Patch'